<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Disabled track -->
    <item android:state_enabled="false">
        <shape android:shape="rectangle">
            <size
                android:width="52dp"
                android:height="30dp" />

            <corners android:radius="15dp" />

            <solid android:color="@color/switch_track_disabled" />

            <stroke
                android:width="1dp"
                android:color="@color/switch_border_disabled" />
        </shape>
    </item>

    <!-- Checked track -->
    <item android:state_checked="true">
        <shape android:shape="rectangle">
            <size
                android:width="52dp"
                android:height="30dp" />

            <corners android:radius="15dp" />

            <solid android:color="@color/brand_olive" />

            <stroke
                android:width="1dp"
                android:color="@color/brand_olive" />
        </shape>
    </item>

    <!-- Unchecked track -->
    <item>
        <shape android:shape="rectangle">
            <size
                android:width="52dp"
                android:height="30dp" />

            <corners android:radius="15dp" />

            <solid android:color="@color/switch_track_off" />

            <stroke
                android:width="1dp"
                android:color="@color/switch_track_border" />
        </shape>
    </item>

</selector>
